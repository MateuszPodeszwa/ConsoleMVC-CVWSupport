// This file has no @model directive — the source generator will skip it.
// The annotator should show a warning about the missing @model.

Console.WriteLine("This view has no model.");
return NavigationResult.Quit();
